# Seed scripts (placeholder)

No seed scripts exist yet. Future reference/demo seed data will be added here after the API/database schema is approved. The current demo data lives in the frontend at `apps/web/src/data/`.
