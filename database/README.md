# MyPetLink Database (placeholder)

Database scripts are **not implemented yet**. No production database exists yet — the current product is frontend-only with local/demo data in `apps/web`.

- `migrations/` — future schema migrations, added after the API/database schema is approved.
- `seed/` — future seed scripts for reference and demo data.
- `docs/` — future database design documentation.

Planned stack: SQL Server with EF Core migrations (managed alongside the future `apps/api` project).

Do not add real SQL migration scripts until the schema is approved.
