-- MyPetLink migration sqlcmd session settings
-- -------------------------------------------
-- Run this file immediately before migration.sql by passing both files to one
-- sqlcmd invocation. These SET options remain in effect for that same session.

SET ANSI_NULLS ON;
SET ANSI_PADDING ON;
SET ANSI_WARNINGS ON;
SET ARITHABORT ON;
SET CONCAT_NULL_YIELDS_NULL ON;
SET QUOTED_IDENTIFIER ON;
SET NUMERIC_ROUNDABORT OFF;
SET XACT_ABORT ON;
GO
