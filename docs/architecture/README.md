# Architecture docs (placeholder)

System architecture documentation will be added here as the platform grows beyond the frontend-only MVP (e.g. when `apps/api` and the database are designed).

For the current frontend architecture, see [`apps/web/docs/AI_AGENT_REFERENCE.md`](../../apps/web/docs/AI_AGENT_REFERENCE.md).
